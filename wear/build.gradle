apply plugin: "com.android.application"

android {
    compileSdk 35

    defaultConfig {
        namespace "at.maui.flopsydroid.android"
        minSdkVersion 25
        targetSdkVersion 35
        versionCode 1
        versionName "1.0"
        signingConfig signingConfigs.debug
    }

    sourceSets {
        main {
            manifest.srcFile 'AndroidManifest.xml'
            java.srcDirs = ['src']
            jniLibs.srcDirs = ['lib']
            aidl.srcDirs = ['src']
            renderscript.srcDirs = ['src']
            res.srcDirs = ['res']
            assets.srcDirs = ['assets']
        }
    }
}

configurations.configureEach {
    resolutionStrategy.force 'org.jetbrains.kotlin:kotlin-stdlib-jdk8:2.2.21'
}

dependencies {
    implementation project(':core')
    implementation "com.badlogicgames.gdx:gdx:1.13.5"
    implementation "com.badlogicgames.gdx:gdx-backend-android:1.13.5"
    implementation "androidx.appcompat:appcompat:1.7.1"
}